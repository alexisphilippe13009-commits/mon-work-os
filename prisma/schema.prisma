generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// C'est le modèle qui manquait !
model AppState {
  id        String   @id @default("global_state")
  data      Json
  updatedAt DateTime @updatedAt
}